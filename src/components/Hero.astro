---
---

<section class="hero" id="hero">
  <div class="container-narrow hero__inner">
    <p class="hero__name">lazy</p>
    <h1 class="hero__title">
      独立开发者，用 AI 造自己想要的工具。
    </h1>
    <p class="hero__lede">
      做过 Swift 编译器、LLVM 后端。现在做手机实验室、个人 Agent 和基础设施 —— 偶尔出门滑雪、潜水、攀岩。
    </p>
    <div class="hero__ctas">
      <a href="/projects/" class="btn btn--primary">看项目</a>
      <a href="#posts" class="btn btn--ghost">读文章</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--s-16) 0 var(--s-12);
  }

  .hero__inner {
    max-width: var(--max-w-narrow);
  }

  .hero__name {
    margin: 0 0 var(--s-4);
    font-size: clamp(22px, 3vw, 28px);
    font-weight: 500;
    letter-spacing: -0.02em;
    color: var(--color-text);
  }

  .hero__title {
    margin: 0 0 var(--s-5);
    font-size: clamp(26px, 3.6vw, 38px);
    font-weight: 500;
    line-height: 1.3;
    letter-spacing: -0.02em;
    color: var(--color-text);
    text-wrap: pretty;
    overflow-wrap: break-word;
  }

  .hero__lede {
    margin: 0 0 var(--s-8);
    max-width: 42ch;
    font-size: 17px;
    line-height: 1.6;
    color: var(--color-text-muted);
  }

  .hero__ctas {
    display: flex;
    gap: var(--s-3);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 20px;
    border-radius: var(--r-md);
    font-size: 15px;
    font-weight: 500;
    transition:
      transform var(--t-fast) var(--ease-out),
      background var(--t-fast) var(--ease-out),
      border-color var(--t-fast) var(--ease-out);
  }

  .btn:hover {
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn--primary {
    background: var(--color-accent);
    color: var(--color-accent-contrast);
  }
  .btn--primary:hover {
    background: var(--color-accent-dark);
  }
  .btn--ghost {
    background: transparent;
    color: var(--color-text);
    border: 1px solid var(--color-border);
  }
  .btn--ghost:hover {
    border-color: var(--color-accent);
    background: var(--color-accent-pale);
  }

  @media (max-width: 600px) {
    .hero {
      padding: var(--s-10) 0 var(--s-8);
    }
    .hero__title br {
      display: none;
    }
  }
</style>
