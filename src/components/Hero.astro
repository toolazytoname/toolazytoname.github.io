---
---

<section class="hero" id="hero">
  <div class="container container-narrow hero__inner">
    <p class="hero__eyebrow eyebrow reveal">lazy · weichao.ren</p>
    <h1 class="hero__title reveal">
      <span class="hero__line">前宇宙大厂基础架构工程师，</span>
      <span class="hero__line">现在是持滑雪、攀岩、游泳教练证的独立开发者。</span>
      <span class="hero__line">用 AI 造自己想要的工具。</span>
    </h1>
    <p class="hero__lede reveal">
      做过 Swift 编译缓存（部分项目编译时长 -50%）、LLVM 跨平台隐私检测工具链。
      14 年写博客，61 篇文章，记录技术与生活。
    </p>
    <div class="hero__ctas reveal">
      <a href="#posts" class="btn btn--primary">读文章</a>
      <a href="/about" class="btn btn--ghost">关于我</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--s-20) 0 var(--s-16);
    border-bottom: 1px solid var(--color-border);
  }

  .hero__inner {
    max-width: 680px;
  }

  .hero__eyebrow {
    color: var(--color-text-dim);
    margin-bottom: var(--s-6);
  }

  .hero__title {
    font-size: clamp(28px, 4.5vw, 48px);
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin: 0 0 var(--s-6);
    color: var(--color-text);
  }

  .hero__line {
    display: block;
  }

  .hero__lede {
    color: var(--color-text-muted);
    font-size: 17px;
    line-height: 1.65;
    margin: 0 0 var(--s-8);
    max-width: 560px;
  }

  .hero__ctas {
    display: flex;
    gap: var(--s-3);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 20px;
    border-radius: var(--r-md);
    font-size: 15px;
    font-weight: 500;
    transition: transform var(--t-fast) var(--ease-out),
      background var(--t-fast) var(--ease-out),
      border-color var(--t-fast) var(--ease-out);
  }

  .btn:hover {
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn--primary {
    background: var(--color-ink);
    color: var(--color-bg);
  }

  .btn--primary:hover {
    background: var(--color-ink-soft);
  }

  .btn--ghost {
    background: transparent;
    color: var(--color-text);
    border: 1px solid var(--color-border);
  }

  .btn--ghost:hover {
    border-color: var(--color-border-strong);
    background: var(--color-bg-subtle);
  }

  @media (max-width: 600px) {
    .hero {
      padding: var(--s-12) 0 var(--s-10);
    }
  }
</style>
