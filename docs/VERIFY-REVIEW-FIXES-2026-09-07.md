# 审查修复验收

2026-09-07，复验应用版本 `5f2d3cc`。结论：上一份全面审查中的四个 P2 问题均已关闭，本次未发现阻断提交的问题。没有修改应用源码。

## 四项回归

| 问题 | 生产构建实测 |
| --- | --- |
| 长中文历史导致持续 413 | 连发三条各 4000 个汉字，随后发送短问题，再发送包含 emoji、反斜杠和引号的消息，五次均 200；请求分别为 12159、24348、24232、24427、32616 字节，均小于 32768；页面保留全部五条用户消息，无错误重试按钮 |
| 413 恢复 | 模拟首次 413，客户端自动降为单条最新问题；请求从 171 字节降到 55 字节，第二次 200，无错误残留 |
| 回复完成抢焦点 | 发送后明确聚焦聊天记录区域，放行延迟响应，焦点仍在记录区域；随后 Escape 关闭，焦点返回 FAB |
| 文章错误链接 | 7 处 Markdown 链接已修正；独立扫描 61 篇构建文章，未发现正文站内路径缺失、同页锚点缺失或重复 ID |
| 代码注释对比度 | Aspects 文章同一注释 token 在 390/768/1440 下均通过 axe color-contrast 检查；前景现为 `#f0f3f6`、背景 `#0a0c10` |

413 恢复是请求预算问题的额外降级验证，上表仍对应四项原始问题。

## 完整检查

- 本轮重新执行 `npm run ci`：8 个测试文件、54 个测试通过；55 个文件的 Astro 检查为 0 errors / warnings / hints；生产构建成功；URL 校验 61 篇、1 个重定向、Failed: 0。
- 390/768/1440 下检查首页、项目、文章列表和关于，共 12 组页面：HTTP 200，无页面异常、横向溢出和已加载破图；空白文章缩略图已移除。查看了手机首页、平板项目页和桌面首页截图。
- Aspects 长文三档宽度无页面横向溢出。代码高亮本次针对原先不达标的注释进行检查，不将其表述为全站无障碍认证。
- 首页项目介绍已精简，二维码作为扫码入口展示；保留当前仅使用 lazy 的站点身份设置。

## 提交与范围

开始复验时工作区干净，`master` 与更新后的 `origin/master` 均指向 `5f2d3cc`。修复已经按构建、聊天、内容、视觉、资源和文档拆分提交并上传，本轮保留现有历史，仅补充这份验收记录和旧报告的状态提示。

测试使用本地生产静态产物和实际打包服务端函数，禁用模型 key；部分故障响应由浏览器模拟。没有调用真实付费模型、重跑干净依赖安装、验证全部外部服务在线状态，或确认 Vercel 部署已上线。

Markdown/Vite 的已知弃用提示仍存在，不影响本轮 CI；它们与其他非阻断优化建议可在后续维护中处理。
