# 四项定点修复复核

## 最新复核：第 1 项指定场景通过

2026-09-06 后续修改完成后，再次使用最新源码的生产构建，以 Chromium 在 1440px 和 390px 下只拦截第一次 mount-chat 请求。两种宽度均确认：失败后按钮恢复，第二次点击发送同一分块路径并带 `?retry=时间戳`，返回 200；聊天面板数量为 1，旧入口移除，“有哪些项目”静态回答正常，无未处理页面异常。

实际请求示例：`/_astro/mount-chat.B1BQZ8R2.js`（首次拦截），随后 `/_astro/mount-chat.B1BQZ8R2.js?retry=1788696352052`（200）。因此上一轮剩余的“首次模块请求失败后再次点击”问题可以关闭，四项定点回归均已通过。

本次隔离副本 `/tmp/weichao-import-retry-exYdP4` 的 `npm run ci` 通过：7 个测试文件、51 个测试；53 个文件的 Astro 检查零诊断；生产构建和 URL helper 测试成功，61 个固定文章页面校验 Failed: 0。复用已安装依赖，未重跑干净安装。未修改应用源码、调用真实模型或部署。

边界：未实测其他浏览器引擎的无 URL 错误及整页刷新分支，也未扩展到依赖分块单独失败的场景。本次通过结论针对用户指定的 mount-chat 首次请求失败恢复。

证据：[请求与结果](audits/2026-09-06-import-retry/results.json)、[手机重试成功](audits/2026-09-06-import-retry/success-390.png)。以下保留上一版复核记录，便于追溯。

## 上一版复核

结论：第 2、3、4 项通过；第 1 项部分修复，实际加载失败后的再次点击仍不能打开聊天。本轮未修改应用代码，只新增核验记录与证据。

## 尚未关闭：聊天模块加载失败后的真实重试

位置：`src/layouts/BaseLayout.astro:134`。

使用最新源码在隔离副本完成生产构建，以 Chromium 在 1440px 和 390px 下验证：仅拦截第一次 `mount-chat.*.js` 请求，之后允许同路径请求通过。

| 状态 | 两个视口的实际结果 |
| --- | --- |
| 首次点击失败后 | disabled=false，aria-label 已更新为“打开聊天助手（加载失败，点击重试）” |
| 再次点击并等待 | 模块请求总数仍为 1，聊天面板数量为 0，入口仍存在 |
| 未拦截的独立页面 | 聊天成功挂载，后续会话测试通过 |

catch 正确恢复了按钮，但同一页面内再次 import 同一个 URL 并没有发出新的请求，失败结果仍被复用。因此“按钮能再点”尚不等于“加载可以重试”。这些结果来自生产构建产物，不涉及开发模式的 `$RefreshSig$`。

建议：失败后提供明确的“刷新重试”，通过页面重新初始化恢复；或者采用能实际重新加载失败模块及其依赖的机制，并用相同的首次请求拦截场景验收。当前失败文案只改了 aria-label，若保留该流程，也应让视觉用户能看见恢复动作。

## 可以关闭的三项

- 旧错误重试：先让“苹果甲”失败，再让“有哪些项目”静态成功，重试按钮数量为 0，两条用户消息均保留。随后“苹果乙”失败并重试成功，用户消息仍为 `[苹果甲, 有哪些项目, 苹果乙]`，没有删除前面的成功对话。
- 重定向：内存注入实际 destination 为 `/` 后，完整校验打印 `redirect target mismatch` 并返回 1。在独立临时配置中让 Vercel 指向 `/`、Astro 指向正确文章，实际解析返回 conflict。未修改项目配置文件。
- YAML 注释：新增的脚本测试验证 `categories: [life, craft] # category notes` 解析为数组，并按首个分类生成 `life/2026/09/06/test/`；带引号的 `#` 也保留。该具体回归通过，不把手写解析器视为完整 YAML 实现。

## 验证范围

隔离副本 `/tmp/weichao-four-fixes-BjDuI7` 复用当前已安装依赖，独立生成构建产物。`npm run ci` 通过：6 个测试文件、48 个 Vitest 测试；51 个文件的 Astro 检查零诊断；生产构建成功；URL helper 测试通过；61 个固定文章页面、1 个重定向 fixture、dist 检查开启、Failed: 0。没有重新执行干净安装。

浏览器仅做这次修复涉及的流程，使用本地模拟 API；没有真实模型调用或线上写入。构建中的 Markdown/Vite 插件弃用提示仍存在，不影响本次 CI 结果。

证据：[浏览器原始结果](audits/2026-09-06-four-fixes/results.json)、[手机加载重试后](audits/2026-09-06-four-fixes/load-retry-390.png)、[对话保留](audits/2026-09-06-four-fixes/conversation.png)。
