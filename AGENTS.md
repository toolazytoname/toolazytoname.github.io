# 项目约定

- 这是 Astro 个人站；内容页优先预渲染，React 用于必要交互。
- 安装与构建命令以 package.json 和经验证的锁文件为准。
  发布前执行 npm run ci（test + build + URL 校验）；安装失败必须报告，不能跳过。
  UI 改动另做短的手机/中间宽度/桌面检查，不必每次全量端到端。
- 新文章放 src/content/posts；项目、近况、问答分别维护 src/data 中对应文件。
- 修改 slug、分类或日期前核对旧 URL，已有公开地址需保留或提供重定向。
- 不编造项目上线状态、用户数、个人经历、证书或产品截图。
- 共享视觉值用 global.css token；允许数据驱动的局部 CSS 变量。
  正文可读性和实际断点表现优先于固定装饰规则。
- UI 改动检查手机、中间宽度、桌面和键盘流程；接口改动检查坏输入与降级。
- Key 仅在服务端使用；外部输入需运行时校验；日志默认不记录完整访客对话。
- 改动架构、目录、环境变量或功能时，同步相应 README/DEPLOY 说明。
- 完成后报告修改、验证结果和未覆盖范围；审查任务区分事实与建议。
